# --- Current: config + dataset access ---
python-dotenv>=1.0.0
datasets>=2.19.0

# --- Document processing / numerics ---
numpy>=1.26.0
pandas>=2.2.0
tqdm>=4.66.0
tiktoken>=0.7.0

# --- Embeddings + vector retrieval ---
sentence-transformers>=3.0.0
faiss-cpu>=1.8.0
torch>=2.2.0

# --- LLM answer generation (API) ---
openai>=1.40.0
httpx>=0.27.0
tenacity>=8.5.0

# --- Backend API ---
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.7.0
pydantic-settings>=2.3.0

# --- Evaluation (RAGAS + metrics) ---
ragas>=0.1.21
scikit-learn>=1.5.0

# --- Testing ---
pytest>=8.2.0
pytest-cov>=5.0.0

# Optional later (only if switching from FAISS to Postgres/pgvector):
# psycopg[binary]>=3.2.0
# pgvector>=0.3.0
