# Optional. Relative paths resolve from the project root.
HOTPOTQA_CACHE_DIR=data/raw/huggingface

# Required later for LLM answer generation (do not commit real keys).
OPENAI_API_KEY=
# OPENAI_MODEL=gpt-4o-mini
